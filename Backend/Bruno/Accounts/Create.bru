meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{PATH}}/accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "parentId": null,
    "name": "Costos"
  }
}
